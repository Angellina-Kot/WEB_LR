RewriteEngine On

# Если файл/папка реально существует — не трогаем
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# Все /api/* отправляем в router.php
RewriteRule ^api(/.*)?$ router.php [QSA,L]
